# module_6

- `comparable_interface.java` demonstrates bubble sort using the `Comparable` interface.
- `comparator_interface.java` demonstrates bubble sort using the `Comparator` interface.
- `unit_test_mod6.java` runs both example `main` methods and asserts their output against expected results.
- `run_mod6_tests.ps1` compiles the module 6 Java files and runs the unit test script.
